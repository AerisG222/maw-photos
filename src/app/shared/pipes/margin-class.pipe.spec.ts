import { MarginClassPipe } from './margin-class.pipe';

describe('MarginClassPipe', () => {
    it('create an instance', () => {
        const pipe = new MarginClassPipe();
        expect(pipe).toBeTruthy();
    });
});
