import { ThemeIsDarkPipe } from './theme-is-dark.pipe';

describe('ThemeIsDarkPipe', () => {
    it('create an instance', () => {
        const pipe = new ThemeIsDarkPipe();
        expect(pipe).toBeTruthy();
    });
});
